# Chunking pipeline (Task 4)
langchain-text-splitters>=0.3
transformers>=4.44  # tokenizer for token-aware chunk sizing (torch not needed until Task 5)

# Notebooks & analysis (Tasks 2+)
jupyter
pandas
matplotlib

# Testing
pytest>=8
